#include<stdio.h>
#include<conio.h>
#include<math.h>

void main(){
int a;
clrscr();
printf("Enter a number to check whether it is absolute value: ");
scanf("%d",&a);
//printf("\n%d",abs(a));

if(a==abs(a)){
printf("\nThe number %d is matched with its absolute value", a);
}
else{
printf("\nThe number %d is not matched with the absolute value", a);
}
getch();
}
